{
  "name": "frida-float-menu",
  "version": "1.0.0",
  "description": "A Frida library for creating interactive floating windows with UI components on Android",
  "license": "MIT",
  "private": false,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "files": [
    "src/**/*.ts",
    "README.md",
    "example.js"
  ],
  "scripts": {
    "build-example": "frida-compile example.js -o example-compiled.js -c",
    "check": "tsc --noEmit",
    "clean": "rm -f _agent.js example-compiled.js"
  },
  "keywords": [
    "frida",
    "android",
    "ui",
    "floating-window",
    "overlay",
    "hooking"
  ],
  "dependencies": {},
  "devDependencies": {
    "@types/frida-gum": "^18.3.1",
    "@types/node": "^18.14.0",
    "frida-compile": "^16.1.8",
    "typescript": "^5.0.0"
  }

}
